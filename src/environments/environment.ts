// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyDuqY4SR3ZxVo9fiIxB6CrkU2loVnUnNDY",
    authDomain: "stream-overlay-9e4d3.firebaseapp.com",
    databaseURL: "https://stream-overlay-9e4d3.firebaseio.com",
    projectId: "stream-overlay-9e4d3",
    storageBucket: "stream-overlay-9e4d3.appspot.com",
    messagingSenderId: "635375058388"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
